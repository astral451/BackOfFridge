# Shared secret required in the x-api-key header for all /api requests,
# and entered once in the web UI (stored in the browser's localStorage).
API_KEY=change-me-to-something-long-and-random

PORT=3000
